<p id="notice"></p>
<div class="container">
  
  <div>
<% if @user.errors.any? %>
  <%= @user.errors.count %>errors prohibited this obj from being saved:
  <ul>
    <% @user.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
  </div>
  
<div class="row">
  <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
    <h2>User info</h2>
    <%= form_with model:@user do |f| %>
    <div class="form-group">
      <label for="name">Name</label>

  <%= f.text_field :name, autofocus: true, id:"name", class: "form-control" %>

    </div>
    <div class="form-group">
      <label for="profile_image">Image</label>
      <%= f.file_field :profile_image, placeholder: "profile_image", accept: "image/*", class: "form-control-file" %>
      </div>
    <div class="form-group">
      <label for="introduction">Introduction</label>

  　　<%= f.text_field :introduction, autofocus: true, id:"introduction", class: "form-control" %>

    </div>
    <div class="form-group">
       <%= f.submit "Update User", class: "btn btn-secondary" %>
    </div>
<% end %>



  </div>

</div>
</div>
